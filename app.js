'use strict';

// SVG size data in pixels
var height = 30;
var fullWidth = 800;
var widthOffset = 125;
var width = fullWidth - widthOffset;
var radius = height / 2;
var dur = 1500;

var app = angular.module('app',['config']);